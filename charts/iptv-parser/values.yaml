image:
  repository: ghcr.io/romaxa55/iptv-parser
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-secret

# Cross-namespace shared infrastructure (from megav namespace)
postgresql:
  host: "megav-heavy.megav.svc.cluster.local"
  port: "5432"
  database: "megav_sources"
  existingSecret: "postgres.megav-heavy.credentials.postgresql.acid.zalan.do"
  usernameKey: "username"
  passwordKey: "password"

redis:
  host: "redis-master.megav.svc.cluster.local"
  port: "6379"
  db: "1"
  existingSecret: "redis"
  passwordKey: "redis-password"

# --- API Server ---
apiService:
  enabled: true
  replicas: 2
  port: 8080
  cache: true
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

# --- M3U Parser CronJob ---
m3uParser:
  enabled: true
  schedule: "0 */1 * * *"
  batchSize: 1000
  fastCheck: "true"
  fastCheckWorkers: 500
  dbWriters: 8
  sourceWorkers: 10
  debug: false
  resources:
    requests:
      cpu: 1000m
      memory: 512Mi
    limits:
      cpu: 4000m
      memory: 2Gi

# --- EPG Parser CronJob ---
epgParser:
  enabled: true
  schedule: "30 */6 * * *"
  batchSize: 500
  debug: false
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi

# --- Stream Checker CronJob ---
channelChecker:
  enabled: true
  schedule: "*/15 * * * *"
  workers: 500
  batch: 5000
  debug: false
  resources:
    requests:
      cpu: 1000m
      memory: 512Mi
    limits:
      cpu: 4000m
      memory: 2Gi

# --- Thumbnail Worker Deployment ---
thumbnailWorker:
  enabled: true
  replicas: 1
  workers: 50
  outputDir: "/app/thumbnails"
  mode: "batch"
  batchSchedule: "*/15 * * * *"
  batchSize: 1000
  resources:
    requests:
      cpu: 500m
      memory: 256Mi
    limits:
      cpu: 2000m
      memory: 1Gi

# --- GitHub Crawler CronJob ---
githubCrawler:
  enabled: true
  schedule: "0 */12 * * *"
  minStars: 0
  maxAgeDays: 7
  minChannels: 5
  debug: false
  githubTokenSecret: "github-crawler-token"
  githubTokenKey: "token"
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

# --- Channel Enricher CronJob ---
channelEnricher:
  enabled: true
  schedule: "0 */6 * * *"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

# --- Ingress ---
ingress:
  enabled: true
  className: nginx
  hosts:
    - host: iptv.megav.app
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: iptv-megav-app-tls
      hosts:
        - iptv.megav.app

# --- Service Account ---
serviceAccount:
  create: true
  name: ""

# --- Thumbnail PVC ---
thumbnailStorage:
  enabled: true
  size: 5Gi
  storageClass: longhorn
  accessModes:
    - ReadWriteOnce
