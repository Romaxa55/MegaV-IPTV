{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "iptv-parser.serviceAccountName" . }}
  labels:
    {{- include "iptv-parser.labels" . | nindent 4 }}
{{- end }}
