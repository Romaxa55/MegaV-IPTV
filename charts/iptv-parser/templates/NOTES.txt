IPTV Backend has been deployed to namespace {{ .Release.Namespace }}.

Components:
{{- if .Values.apiService.enabled }}
  - API Server: {{ .Values.apiService.replicas }} replicas on port {{ .Values.apiService.port }} (with playlist/EPG sync)
{{- end }}
{{- if .Values.thumbnailWorker.enabled }}
  - Thumbnail Worker: {{ .Values.thumbnailWorker.replicas }} replicas
{{- end }}

Connections:
  - PostgreSQL: {{ .Values.postgresql.host }}:{{ .Values.postgresql.port }}/{{ .Values.postgresql.database }}
  - IPTV URLs Secret: {{ .Values.iptv.existingSecret }}

Useful commands:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=api -f

Trigger manual sync:
  curl -X POST http://<api-host>/api/sync/all
